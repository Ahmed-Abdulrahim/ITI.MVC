
@model IEnumerable<Department>
@{
    ViewData["Title"] = "Index";
}

<h1>All Department</h1>
<a asp-action="Create" class="btn btn-primary m-5">Create Department</a>
<table class="table table-bordered table-hover text-center">
    <thead>
        <tr>
            <th>Name</th>
            <th>Capacity</th>
            <th>Details</th>
            <th>Update</th>
            <th>Delete</th>

        </tr>
    </thead>
    <tbody>
        @if (Model.Count() > 0)
        {
            foreach (var item in Model)
            {
                <tr>
                    <td>@item.Name</td>
                    <td>@item.Capacity</td>
                    <td>
                        <a asp-action="Details" class="btn btn-warning" asp-route-id="@item.Id">Details</a>
                    </td>
                    <td>
                        <a asp-action="Edit" class="btn btn-success" asp-route-id="@item.Id">Update</a>
                    </td>
                    <td>
                        <form asp-action="Delete" method="post" asp-route-id="@item.Id">
                            <button type="submit" class="btn btn-danger">Delete</button>
                        </form>

                    </td>
                </tr>
            }
        }
        else
        {
            <tr>
                <td colspan="5">
                    <h4 class="text-danger"> There is No Department :(</h4>
                </td>
            </tr>
        }
    </tbody>
</table>

